apiVersion: v1
kind: Service
metadata:
  name: {{ include "videokit.fullname" . }}
  labels:
    app: {{ include "videokit.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "videokit.fullname" . }}
    component: server